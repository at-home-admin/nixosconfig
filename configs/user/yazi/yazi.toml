[manager]
ratio = [1, 3, 4]
sort_by = "natural"
show_hidden = false

[preview]
image_filter = "triangle"
image_quality = 75

[opener]
edit = [{ run = 'nvim "$@"', block = true, for = "unix" }]

[open]
rules = [{ mime = "text/*", use = ["edit"] }]
